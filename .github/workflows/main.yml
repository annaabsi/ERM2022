# name: scraper-daily

# on:
#   # Triggers the workflow on push or pull request events but only for the main branch
#   push:
#     branches: [ main ]
#   schedule:
#     - cron: '0 0-23 * * *' # runs every hour

# jobs:
#   scraper:
#     runs-on: ubuntu-latest
#     steps:

#       - name: checkout repo content
#         uses: actions/checkout@v2 # checkout the repository content to github runner

#       - name: setup python
#         uses: actions/setup-python@v2
#         with:
#           python-version: '3.9.7' # install the python version needed

#       - name: execute my notebook
#         run: |
#           python -m pip install requests jupyter nbconvert nbformat
#           jupyter nbconvert --to notebook --execute lima_scraper.ipynb --TemplateExporter.exclude_input=True --ExecutePreprocessor.kernel_name='python3' --output lima_scraper_ejecutado      

#       - name: commit my files
#         run: |
#           git pull
#           git config --local user.email "action@github.com"
#           git config --local user.name "GitHub Action"
#           git add -A
#           git commit --allow-empty -m "update data onpe lima" -a

#       - name: push my changes
#         uses: ad-m/github-push-action@v0.6.0
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}
#           branch: main
